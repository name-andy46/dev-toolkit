# Tasks: <short title>

Implementation checklist for this change. `spec-archive` refuses to finalize while any box is
unchecked, so keep this honest — check items only when actually done.

- [ ] <first task>
- [ ] <second task>
- [ ] Update the delta spec(s) if implementation revealed a different requirement
- [ ] <...>
