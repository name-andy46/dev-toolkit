> **The command center.** Everything still open lives here, grouped under the project it belongs
> to. The daily logs in `01_Logs/` are the diary — what happened on a given day. This file is the
> list of what's left.
>
> Top-level headings are **projects, never dates** — `# website-redesign`, `# PROJ-118 — null id
> on CSV export`. The date rides the item, not the heading. The `update-daily-note` and
> `cleanup-daily-note` skills both write here and follow the same placement rules.
>
> Delete this note once you've added your first project.
